name: showmovies
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.13.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  dio: ^5.11.1
  flutter_bloc: ^9.1.1
  bloc_test: ^10.0.0
  mocktail: ^1.0.5
  firebase_core: ^3.3.0 
  firebase_auth: ^5.1.1
  
  # Utilisation des paquets communautaires exacts
  isar_community: ^3.3.2
  isar_community_flutter_libs: ^3.3.2
  
  internet_connection_checker_plus: ^3.1.2
  path_provider: ^2.1.6
  get_it: ^9.3.0
  go_router: ^18.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.8 
  
  # Le générateur associé à la communauté
  isar_community_generator: ^3.3.2

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
